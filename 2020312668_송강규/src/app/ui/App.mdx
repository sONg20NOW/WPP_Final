export const MyComponent = ({ toggle }) => {
  return toggle ? (
    <div>
      **This is Markdown content!**
    </div>
  ) : (
    <div>
      This is plain text content.
    </div>
  );
};

<MyComponent toggle={true} />
